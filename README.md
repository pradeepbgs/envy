#### here we will create a secure .env sync application where we can store our encrypeted .env on s3 or r2 and before syncing user will create a secret whcih
will be stored in user desk and while syncing we will download the encryped .env and decrypt and replace with user given .env file 
